// This file serves as documentation for the imports needed in the project
// It is not actually used in the code, but serves as a reference

// Core imports
import SwiftUI
import Foundation
import Observation // For @Observable macro

// Required for the CalendarViewModel
// No additional imports needed beyond Observation for @Observable macro
// Note: Combine is not needed when using @Observable

// Required for the Guest model
// No additional imports needed beyond Foundation and SwiftUI

// Required for UI components
// No additional imports needed beyond SwiftUI

// Note: Make sure all files have the appropriate imports at the top
// Most files will only need `import SwiftUI`
// Files that use @Bindable will need `import Observation` 